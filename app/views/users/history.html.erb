<h1>My History</h1>

<h3>My Completed Requests</h3>

<div class="row">
    <% @request.each do |item| %>

    <% if item.status.capitalize == 'Completed' %>

    <div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading warning-color text-center">
                <h4>"<%=item.description%>"</h4>
            </div>
            <div class="panel-body text-center">

                <div class="view overlay hm-zoom hm-black-light">
                    <iframe
                        class="user-map z-depth-1"
                        frameborder="0"
                        style="border:0; width:100%; height:300px"
                        src="https://www.google.com/maps/embed/v1/directions?key=AIzaSyByUpVKGIl-ZT0MxU0yJBBIDdL_SIEVrJg&origin=My+Location&destination=<%=item.event.location%>"
                        allowfullscreen></iframe>
                    <div class="mask flex-center">
                        <h1 class="white-text"><%=item.event.name%>, $<%= item.cost %>0</h1>
                    </div>
                </div>

                <p>
                    Requestor:
                    <%= item.requestor.name %>
                    <br>Event:
                    <%=item.event.name%>
                    <br>Cost: $<%= item.cost %>0
                    <br>Status:
                    <%=item.status.capitalize!%></p>

                <%= link_to "View", request_path(item.id), class: "btn blue col-sm-12", style: "margin:0" %>

            </div>
        </div>
    </div>
    <% end %>
    <%end%>
</div>

<h3>My Completed Standins</h3>

<div class="row">
    <% @standin.each do |item| %>

    <% if item.status.capitalize == 'Completed' %>

    <div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading warning-color text-center">
                <h4>"<%=item.description%>"</h4>
            </div>
            <div class="panel-body text-center">

                <div class="view overlay hm-zoom hm-black-light">
                    <iframe
                        class="user-map z-depth-1"
                        frameborder="0"
                        style="border:0; width:100%; height:300px"
                        src="https://www.google.com/maps/embed/v1/directions?key=AIzaSyByUpVKGIl-ZT0MxU0yJBBIDdL_SIEVrJg&origin=My+Location&destination=<%=item.event.location%>"
                        allowfullscreen></iframe>
                    <div class="mask flex-center">
                        <h1 class="white-text"><%=item.event.name%>, $<%= item.cost %>0</h1>
                    </div>
                </div>

                <p>
                    Requestor:
                    <%= item.requestor.name %>
                    <br>Event:
                    <%=item.event.name%>
                    <br>Cost: $<%= item.cost %>0
                    <br>Status:
                    <%=item.status.capitalize!%></p>

                <%= link_to "View", request_path(item.id), class: "btn blue col-sm-12", style: "margin:0" %>

            </div>
        </div>
    </div>
    <% end %>
    <%end%>
</div>

<h3>My Cancelled Requests</h3>
<p>These are the requests you cancelled:</p>

<div class="row">
    <% @request.each do |item| %>

    <% if item.status.capitalize == 'Cancelled' || item.status.capitalize == 'Event cancelled' %>

    <div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading warning-color text-center">
                <h4>"<%=item.description%>"</h4>
            </div>
            <div class="panel-body text-center">

                <div class="view overlay hm-zoom hm-black-light">
                    <iframe
                        class="user-map z-depth-1"
                        frameborder="0"
                        style="border:0; width:100%; height:300px"
                        src="https://www.google.com/maps/embed/v1/directions?key=AIzaSyByUpVKGIl-ZT0MxU0yJBBIDdL_SIEVrJg&origin=My+Location&destination=<%=item.event.location%>"
                        allowfullscreen></iframe>
                    <div class="mask flex-center">
                        <h1 class="white-text"><%=item.event.name%>, $<%= item.cost %>0</h1>
                    </div>
                </div>

                <p>
                    Requestor:
                    <%= item.requestor.name %>
                    <br>Event:
                    <%=item.event.name%>
                    <br>Cost: $<%= item.cost %>0
                    <br>Status:
                    <%=item.status.capitalize!%></p>

                <%= link_to "View", request_path(item.id), class: "btn blue col-sm-12", style: "margin:0" %>

            </div>
        </div>
    </div>
    <% end %>
    <%end%>
</div>

<h3>My Expired Requests</h3>
<p>There were no standins for these requests. Sorry!</p>

<div class="row">
    <% @request.each do |item| %>

    <% if item.status.capitalize == 'New' && item.event.date < Date.today %>

    <div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading warning-color text-center">
                <h4>"<%=item.description%>"</h4>
            </div>
            <div class="panel-body text-center">

                <div class="view overlay hm-zoom hm-black-light">
                    <iframe
                        class="user-map z-depth-1"
                        frameborder="0"
                        style="border:0; width:100%; height:300px"
                        src="https://www.google.com/maps/embed/v1/directions?key=AIzaSyByUpVKGIl-ZT0MxU0yJBBIDdL_SIEVrJg&origin=My+Location&destination=<%=item.event.location%>"
                        allowfullscreen></iframe>
                    <div class="mask flex-center">
                        <h1 class="white-text"><%=item.event.name%>, $<%= item.cost %>0</h1>
                    </div>
                </div>

                <p>
                    Requestor:
                    <%= item.requestor.name %>
                    <br>Event:
                    <%=item.event.name%>
                    <br>Cost: $<%= item.cost %>0</p>

                <%= link_to "View", request_path(item.id), class: "btn blue col-sm-12", style: "margin:0" %>

            </div>
        </div>
    </div>
    <% end %>
    <%end%>
</div>
